/*
 * 原 `AnimalRestController` 已迁移到
 *   src/spring/java/org/example/rest/AnimalRestController.java
 * 该目录属于可选的 Spring Boot 构建 profile（spring-boot），默认不会被普通构建编译。
 *
 * 为避免在默认构建中引入依赖或产生类冲突，这个文件已清空并保留说明。
 */
package org.example;

// ...此处故意不放置任何类定义，以免与 src/spring/java 下的 Spring 控制器发生冲突。
